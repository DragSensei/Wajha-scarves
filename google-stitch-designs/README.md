# Google Stitch HTML Designs

Extract the `.zip` file containing your Google Stitch HTML designs directly into this folder.

Once extracted, we can convert these HTML/CSS designs into React components and integrate them into our feature-based architecture (e.g. inside `features/products/components/` or `shared/components/`).
